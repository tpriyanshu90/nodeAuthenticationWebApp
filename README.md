# nodeAuthenticationWebApp
An authentication Web app made using nodejs and passportJS

# Dependencies
run `npm i` in the root directory

# Start
run `npm start` in root repository

# Browse
project will run on http://localhost://8000
